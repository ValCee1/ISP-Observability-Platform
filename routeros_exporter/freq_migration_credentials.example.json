{
  "_comment": "Copy to secrets/freq_migration_credentials.json (gitignored) and fill in. `cpe_shared` is the one username/password used across every CPE (confirmed 2026-09-16). `sectors` is keyed by sector name from freq_migration_sectors.yml, one credential per sector, same pattern as routeros_api_credentials.json.",
  "cpe_shared": { "username": "CHANGE-ME", "password": "CHANGE-ME" },
  "sectors": {
    "sector1-tower": { "username": "CHANGE-ME", "password": "CHANGE-ME" }
  }
}
