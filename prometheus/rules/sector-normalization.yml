# Recording rules for MikroTik PtMP sectors. Raw metrics come from the
# `mikrotik_sector` SNMP module (see snmp_exporter/snmp.yml); everything
# here is derived. Join key across the whole set is the `sector` label.
#
# Design notes:
#  - Sector RF throughput is built from the per-CPE byte counters, NOT the
#    wlan interface counter - the interface name varies per device and can
#    come back blank over SNMP, whereas summing the registration table is
#    always correct. The backhaul (ether1) side still uses the interface
#    counter since that's the only source for it.
#  - CCQ is absent under Nv2, so link quality is expressed via SNR and PHY
#    rate, not CCQ.
#  - The alignment score mirrors the link_endpoint_health_score approach in
#    link-normalization.yml: start at 100, subtract capped penalties, so no
#    single dimension can zero out an otherwise-fine CPE.

groups:
  - name: sector-availability
    rules:
      - record: sector_up
        expr: up{job="snmp-sectors"}

  - name: sector-aggregates
    rules:
      # Registered CPE count per sector (one wlan per sector in practice;
      # max collapses multi-wlan devices to the busiest).
      - record: sector:clients
        expr: max by (sector, site) (sector_registered_clients)

      # RF throughput; sum of the per-CPE byte counters, both directions.
      - record: sector:rf_downlink_bps
        expr: sum by (sector, site) (rate(sector_cpe_tx_bytes[5m])) * 8

      - record: sector:rf_uplink_bps
        expr: sum by (sector, site) (rate(sector_cpe_rx_bytes[5m])) * 8

      # Total offered load on the sector, in Mbps - the number the capacity
      # thresholds in sector-thresholds.yml are expressed against.
      - record: sector:throughput_mbps
        expr: (sector:rf_downlink_bps + sector:rf_uplink_bps) / 1e6

      # Backhaul side: highest-throughput non-wireless interface on the
      # sector device (ether1 in almost every case). topk keeps it robust
      # to blank/odd ifName values.
      - record: sector:backhaul_tx_bps
        expr: >
          topk by (sector, site) (1,
            rate(sector_interface_out_octets{ifName!~"wlan.*|lo|bridge.*|"}[5m]) * 8)

      - record: sector:backhaul_rx_bps
        expr: >
          topk by (sector, site) (1,
            rate(sector_interface_in_octets{ifName!~"wlan.*|lo|bridge.*|"}[5m]) * 8)

      - record: sector:worst_cpe_signal_dbm
        expr: min by (sector, site) (sector_cpe_signal_dbm)

      # Backhaul port capacity + utilisation. ifHighSpeed is in Mbit/s;
      # topk-1 the non-wireless interface to match sector:backhaul_*_bps.
      - record: sector:backhaul_capacity_bps
        expr: >
          topk by (sector, site) (1,
            sector_interface_speed_mbps{ifName!~"wlan.*|lo|bridge.*|"} * 1e6)

      - record: sector:backhaul_utilization_percent
        expr: >
          (sector:backhaul_tx_bps > sector:backhaul_rx_bps or sector:backhaul_rx_bps)
          / on (sector, site) sector:backhaul_capacity_bps * 100

  - name: sector-cpe
    rules:
      - record: sector:cpe:tx_bps
        expr: rate(sector_cpe_tx_bytes[5m]) * 8

      - record: sector:cpe:rx_bps
        expr: rate(sector_cpe_rx_bytes[5m]) * 8

      # Absolute dB gap between the two receive chains. A large gap means a
      # connector / pigtail / polarity / alignment fault on one chain.
      - record: sector:cpe:chain_imbalance_db
        expr: abs(sector_cpe_rx_strength_ch0_dbm - sector_cpe_rx_strength_ch1_dbm)

      # Element-wise min (most negative) of the two receive chains - the
      # limiting chain for this CPE's link budget. PromQL has no element-wise
      # min across two metric names, hence the bool-mask arithmetic.
      - record: sector:cpe:worst_chain_dbm
        expr: |
          (sector_cpe_rx_strength_ch0_dbm <= bool sector_cpe_rx_strength_ch1_dbm) * sector_cpe_rx_strength_ch0_dbm
          + (sector_cpe_rx_strength_ch0_dbm > bool sector_cpe_rx_strength_ch1_dbm) * sector_cpe_rx_strength_ch1_dbm

  # Baseline-relative CPE metrics. Split into their own 5m group: the 7d
  # windows (via 10m subqueries to stay cheap) and 24h counts are far too
  # heavy to run at the 15s default.
  - name: sector-cpe-derived
    interval: 5m
    rules:
      - record: sector:cpe:signal_avg7d
        expr: avg_over_time(sector_cpe_signal_dbm[7d:10m])

      - record: sector:cpe:signal_drift_db
        expr: sector_cpe_signal_dbm - sector:cpe:signal_avg7d

      - record: sector:cpe:worst_chain_avg7d
        expr: avg_over_time(sector:cpe:worst_chain_dbm[7d:10m])

      - record: sector:cpe:chain_drift_db
        expr: sector:cpe:worst_chain_dbm - sector:cpe:worst_chain_avg7d

      # 1 once a CPE has ~17h+ of signal history in the last 24h - the
      # drift / rising alerts gate on this so they don't fire off a
      # baseline that's really just "the last few hours".
      - record: sector:cpe:baseline_ready
        expr: (count_over_time(sector_cpe_signal_dbm[24h:5m]) > bool 200)

      # 0-100 physical-alignment / link-quality score per CPE.
      # Penalty budget: weak limiting chain up to 45, chain imbalance up to
      # 25, low SNR up to 30, downward chain drift vs 7d up to 30.
      - record: sector:cpe:alignment_score
        expr: |
          clamp_min(
            100
            - clamp_max(clamp_min((- sector:cpe:worst_chain_dbm) - 65, 0) * 4, 45)
            - clamp_max(clamp_min(sector:cpe:chain_imbalance_db - 3, 0) * 3, 25)
            - clamp_max(clamp_min(25 - sector_cpe_snr_db, 0) * 2, 30)
            - clamp_max(clamp_min((- sector:cpe:chain_drift_db) - 3, 0) * 3, 30),
            0
          )

      # Per-sector counts of CPEs in trouble - the numbers behind the
      # "widespread degradation" rollup alert and the NOC sector row.
      - record: sector:degraded_cpe_count
        expr: >
          count by (sector, site) (sector:cpe:alignment_score < 60)
          or (0 * max by (sector, site) (sector_up))

      - record: sector:critical_cpe_count
        expr: >
          count by (sector, site) (sector:cpe:alignment_score < 40)
          or (0 * max by (sector, site) (sector_up))

  # 24h rollups for the capacity digest and the peak-hour panels. Evaluated
  # every 5m (not every 15s) - the [24h:1m] subqueries are expensive.
  - name: sector-24h
    interval: 2m
    rules:
      - record: sector:throughput_mbps:avg24h
        expr: avg_over_time(sector:throughput_mbps[24h])

      - record: sector:throughput_mbps:max24h
        expr: max_over_time(sector:throughput_mbps[24h])

      - record: sector:throughput_mbps:p95_24h
        expr: quantile_over_time(0.95, sector:throughput_mbps[24h])

      - record: sector:clients:max24h
        expr: max_over_time(sector:clients[24h])

      - record: sector:clients:p95_24h
        expr: quantile_over_time(0.95, sector:clients[24h])

      # Minutes in the last 24h the sector was over its warn threshold -
      # this is what the 08:00 digest reports ("above budget for N min,
      # 24h avg X Mbps").
      - record: sector:minutes_over_cap_mbps:24h
        expr: sum_over_time((sector:throughput_mbps > bool on(sector) sector:cap_mbps_warn)[24h:1m])

      - record: sector:minutes_over_cap_clients:24h
        expr: sum_over_time((sector:clients >= bool on(sector) sector:cap_clients_warn)[24h:1m])

  # Hourly averages, kept as a low-resolution series so the dashboard can
  # show the daily traffic/load rhythm ("busy hours") over the last week
  # without a heavy subquery at render time. Evaluated every 30m.
  - name: sector-hourly
    interval: 5m
    rules:
      - record: sector:throughput_mbps:hourly
        expr: avg_over_time(sector:throughput_mbps[1h])

      - record: sector:clients:hourly
        expr: avg_over_time(sector:clients[1h])

      - record: sector:cpu_percent:hourly
        expr: avg_over_time(max by (sector, site) (sector_cpu_load_percent)[1h:1m])

      - record: sector:backhaul_bps:hourly
        expr: avg_over_time((sector:backhaul_tx_bps + sector:backhaul_rx_bps)[1h:1m])

  # Throughput / client count / backhaul %, averaged by LOCAL HOUR OF DAY
  # (Africa/Lagos = UTC+1, hence time()+3600) over the last 7 days - 24
  # series per sector, hod label 00..23, so a bar chart answers 'which
  # hours is this sector busiest'. One rule per hour because PromQL
  # cannot derive an hour-of-day label from a timestamp.
  - name: sector-hour-of-day
    interval: 15m
    rules:
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 0))[7d:15m])
        labels: { hod: "00" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 1))[7d:15m])
        labels: { hod: "01" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 2))[7d:15m])
        labels: { hod: "02" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 3))[7d:15m])
        labels: { hod: "03" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 4))[7d:15m])
        labels: { hod: "04" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 5))[7d:15m])
        labels: { hod: "05" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 6))[7d:15m])
        labels: { hod: "06" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 7))[7d:15m])
        labels: { hod: "07" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 8))[7d:15m])
        labels: { hod: "08" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 9))[7d:15m])
        labels: { hod: "09" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 10))[7d:15m])
        labels: { hod: "10" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 11))[7d:15m])
        labels: { hod: "11" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 12))[7d:15m])
        labels: { hod: "12" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 13))[7d:15m])
        labels: { hod: "13" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 14))[7d:15m])
        labels: { hod: "14" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 15))[7d:15m])
        labels: { hod: "15" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 16))[7d:15m])
        labels: { hod: "16" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 17))[7d:15m])
        labels: { hod: "17" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 18))[7d:15m])
        labels: { hod: "18" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 19))[7d:15m])
        labels: { hod: "19" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 20))[7d:15m])
        labels: { hod: "20" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 21))[7d:15m])
        labels: { hod: "21" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 22))[7d:15m])
        labels: { hod: "22" }
      - record: sector:throughput_mbps:hod
        expr: avg_over_time((sector:throughput_mbps and on() (hour(vector(time() + 3600)) == 23))[7d:15m])
        labels: { hod: "23" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 0))[7d:15m])
        labels: { hod: "00" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 1))[7d:15m])
        labels: { hod: "01" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 2))[7d:15m])
        labels: { hod: "02" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 3))[7d:15m])
        labels: { hod: "03" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 4))[7d:15m])
        labels: { hod: "04" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 5))[7d:15m])
        labels: { hod: "05" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 6))[7d:15m])
        labels: { hod: "06" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 7))[7d:15m])
        labels: { hod: "07" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 8))[7d:15m])
        labels: { hod: "08" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 9))[7d:15m])
        labels: { hod: "09" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 10))[7d:15m])
        labels: { hod: "10" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 11))[7d:15m])
        labels: { hod: "11" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 12))[7d:15m])
        labels: { hod: "12" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 13))[7d:15m])
        labels: { hod: "13" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 14))[7d:15m])
        labels: { hod: "14" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 15))[7d:15m])
        labels: { hod: "15" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 16))[7d:15m])
        labels: { hod: "16" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 17))[7d:15m])
        labels: { hod: "17" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 18))[7d:15m])
        labels: { hod: "18" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 19))[7d:15m])
        labels: { hod: "19" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 20))[7d:15m])
        labels: { hod: "20" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 21))[7d:15m])
        labels: { hod: "21" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 22))[7d:15m])
        labels: { hod: "22" }
      - record: sector:clients:hod
        expr: avg_over_time((sector:clients and on() (hour(vector(time() + 3600)) == 23))[7d:15m])
        labels: { hod: "23" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 0))[7d:15m])
        labels: { hod: "00" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 1))[7d:15m])
        labels: { hod: "01" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 2))[7d:15m])
        labels: { hod: "02" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 3))[7d:15m])
        labels: { hod: "03" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 4))[7d:15m])
        labels: { hod: "04" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 5))[7d:15m])
        labels: { hod: "05" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 6))[7d:15m])
        labels: { hod: "06" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 7))[7d:15m])
        labels: { hod: "07" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 8))[7d:15m])
        labels: { hod: "08" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 9))[7d:15m])
        labels: { hod: "09" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 10))[7d:15m])
        labels: { hod: "10" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 11))[7d:15m])
        labels: { hod: "11" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 12))[7d:15m])
        labels: { hod: "12" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 13))[7d:15m])
        labels: { hod: "13" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 14))[7d:15m])
        labels: { hod: "14" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 15))[7d:15m])
        labels: { hod: "15" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 16))[7d:15m])
        labels: { hod: "16" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 17))[7d:15m])
        labels: { hod: "17" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 18))[7d:15m])
        labels: { hod: "18" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 19))[7d:15m])
        labels: { hod: "19" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 20))[7d:15m])
        labels: { hod: "20" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 21))[7d:15m])
        labels: { hod: "21" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 22))[7d:15m])
        labels: { hod: "22" }
      - record: sector:backhaul_utilization_percent:hod
        expr: avg_over_time((sector:backhaul_utilization_percent and on() (hour(vector(time() + 3600)) == 23))[7d:15m])
        labels: { hod: "23" }
