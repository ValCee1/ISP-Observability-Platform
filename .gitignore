secrets/
.env
prometheus_data/
grafana_data/
.DS_Store

# routeros-exporter: local venv + the filled-in (non-example) config
routeros_exporter/.venv/
routeros_exporter/config.yml
routeros_exporter_data/

# freq_migration: local sector inventory (credentials live under secrets/,
# already ignored above)
routeros_exporter/freq_migration_sectors.yml

# python
__pycache__/
*.pyc
.pytest_cache/
